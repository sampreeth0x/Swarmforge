# Target Repo (fixture)

A minimal page renderer used as the swarm's demo target.

Missions you can try against it:

- "Add dark mode support: implement the `dark` theme (static/dark.css) and make
  `render_page` validate the theme argument."
- "Add a `/health` route module."

Run tests: `python -m pytest tests/ -q`